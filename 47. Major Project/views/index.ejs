<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
</head>
<body>
    <h2>All Places</h2>
    <form action="/new" method="GET" >
        <button>Create a new Place Listing</button>
    </form>
    <ul>
        <% allListings.forEach(place=>{ %>
            <li>
                <form action='/listings/edit/<%=place._id%>' method="GET" style="display: inline;">
                    <button class="material-symbols-outlined" style="background: transparent;border: none;font-size: 1em;cursor: pointer;">
                        edit
                    </button>
                </form>
                <form action='/listings/<%=place._id%>?_method=DELETE' method="POST" style="display: inline;">
                    <button class="material-symbols-outlined" style="background: transparent;border: none;font-size: 1em;cursor: pointer;">
                        delete
                    </button>
                </form>
                <form action='/listings/<%=place._id%>' method="GET" style="display: inline;">
                    <button>
                        <%= place.title %>
                    </button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>